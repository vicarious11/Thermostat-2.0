{
  "dof": 5,
  "controls": {
    "actuator": {
      "%torealvalue": "<<expression>>",
      "curves": {
        "increasing": {
          "triggerExpr": "observation > (setpoint + offset)",
          "endExpr": "False"
        },
        "decreasing": {
          "triggerExpr": "(setpoint - offset) < observation < (setpoint + offset)",
          "endExpr": "observation < (setpoint-offset)"
        }
      }
    },
    "vfd": {
      "%torealvalue": "<<expression>>",
      "curves": {
        "increasing": {
          "triggerExpr": "observation > (setpoint + offset)",
          "endExpr": "False"
        },
        "decreasing": {
          "triggerExpr": "(setpoint - offset) < observation < (setpoint + offset)",
          "endExpr": "observation < (setpoint-offset)"
        }
      }
    }
  }
}
